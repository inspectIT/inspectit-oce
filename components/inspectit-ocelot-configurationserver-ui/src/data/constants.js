/**
 * Various constants used by the application.
 */

/**
 * Default <title> content
 */
export const BASE_PAGE_TITLE = "inspectIT Ocelot Configuration Server";

/**
 * Default time buffer in millis which is used to determine the next interval to renew the token.
 */
export const RENEW_TOKEN_TIME_BUFFER = 300000;

/**
 * Default time in millis between the next retry, after a renew token action has failed.
 */
export const RENEW_TOKEN_RETRY_INTERVAL = 60000;

